# TEMPLATE for Traefik Swarm deployment
# Copy to .env on the target Swarm manager and adjust values.

# Where to store Traefik data (ACME + dynamic config).
TRAEFIK_DATA_ROOT=/srv/swarm/prod/traefik/traefik-prod-1

# Hostname of the node where Traefik will run.
TRAEFIK_NODE_HOSTNAME=swarm-02

# Timezone for logs and certificates.
TRAEFIK_TZ=Europe/Warsaw

# Published ports on the Swarm node (temporary test ports).
TRAEFIK_HTTP_PORT=8080
TRAEFIK_HTTPS_PORT=8443
TRAEFIK_DASHBOARD_PORT=9090

# DNS and ACME settings.
TRAEFIK_DOMAIN_ROOT=home.mojadomena.com
TRAEFIK_DASHBOARD_HOST=traefik.${TRAEFIK_DOMAIN_ROOT}
TRAEFIK_ACME_EMAIL=your-email@example.com

# Name of the Swarm secret that contains the Cloudflare API token.
TRAEFIK_CF_SECRET_NAME=cf_api_token
